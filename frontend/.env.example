# Copy to .env.local for local dev, or set these in Render (frontend) before npm run build.
# Vite bakes values in at build time — changing env alone without rebuild has no effect.

VITE_API_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000
